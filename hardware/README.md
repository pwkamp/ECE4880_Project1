This is where all the schematics/etc relating to the hardware implementation will be stored

<!-- TODO(docs-audit): This file is still the original placeholder, but the
     physical build (battery box, ESP32, two DS18B20 probes wired to
     GPIO14/GPIO27, HD44780 16x2 LCD on GPIO16/17/18/19/21/23) is now the
     default, working production configuration per firmware/sdkconfig.defaults
     and firmware/main/src/real_temperature_sensors.c /
     firmware/main/src/real_display.c. Deciding what belongs here (schematic,
     BOM, wiring diagram, photos) is a judgment call for a human, not
     something to fabricate in a docs-accuracy pass. -->