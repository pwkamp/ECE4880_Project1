# SYS-01 — Complete Data Path

For both real sensors, record a distinguishable physical stimulus and compare the ESP32/LCD value and status with BLE trace, backend record, MySQL row, sample API, and browser readout/graph. Preserve one correlated evidence set per sensor. Every hop—sensor → ESP32 → BLE → backend → MySQL → Node API → browser—must be observed; simulated input does not pass the physical system requirement.
